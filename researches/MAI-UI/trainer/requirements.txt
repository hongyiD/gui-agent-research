# Core dependencies for MAI-UI Training
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0

# Training frameworks
trl>=0.7.0

# Parameter-efficient fine-tuning (LoRA)
peft>=0.7.0
bitsandbytes>=0.41.0  # For 4-bit quantization (optional)

# Data processing
pillow>=10.0.0
pyyaml>=6.0
tqdm>=4.66.0

# Utilities
# NumPy 2.x is incompatible with pyarrow/pandas compiled with NumPy 1.x
# Downgrade to NumPy 1.x until all dependencies support NumPy 2.x
numpy>=1.24.0,<2.0.0
pandas>=2.0.0

# Optional: For RL training
# verl  # Uncomment if using verl for RL

# Development
pytest>=7.0.0
black>=23.0.0
ruff>=0.1.0
